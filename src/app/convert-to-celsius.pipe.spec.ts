import { ConvertToCelsiusPipe } from './convert-to-celsius.pipe';

describe('ConvertToCelsiusPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToCelsiusPipe();
    expect(pipe).toBeTruthy();
  });
});
